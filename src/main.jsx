import React from "react";
import ReactDOM from "react-dom/client";
import MemeCoinGame from "./MemeCoinGame";
import "./index.css"; // Tailwind entry

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MemeCoinGame />
  </React.StrictMode>
);
